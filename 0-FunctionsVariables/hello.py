def hello(to):
	to = to.strip().title()
	print(f"Hello, {name}!")


name = input("What's your name? ")  # Ask the user for their name

  # Clean up the name
# first, last = name.split(" ")  # Split the name into first and last
print(f'Hello, "{name}"!')  # Print the user's name
